apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spin-myapp.fullname" . }}-test-connection"
  labels:
    {{- include "spin-myapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spin-myapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
